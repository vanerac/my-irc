/*
** EPITECH PROJECT, 2021
** C
** File description:
** send.c file
*/

#include <commands.h>

//static bool find_by_uuid(void *it, void *data)
//{
//
//}

void command_send(t_global *global, session_t *session,
    char **args
)
{
    (void) session, (void) args, (void) global;
//    uuid target;
//    char *message = args[1];
//    uuid_parse(args[0], target);

//    list_t *dm = node_find_fn(global->private_message, &find_by_uuid, target)

    // find dm structure
    // if none, create one
    // create message and add

//    return SUCCESS;
}